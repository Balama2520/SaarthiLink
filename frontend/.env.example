# Public browser configuration only. Never put server credentials in this file.
# Netlify should set this to the HTTPS URL of the always-on FastAPI service.
# Leave unset only when a same-origin reverse proxy serves /api.
VITE_API_BASE_URL="https://api.example.com"
