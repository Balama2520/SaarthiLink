# Kubernetes Manifests

This directory contains the Kubernetes deployment configuration for Buddy AI.

- `backend-deployment.yaml`: FastAPI backend deployment.
- `frontend-deployment.yaml`: Nginx frontend deployment.
- `ollama-deployment.yaml`: Ollama AI engine deployment.
- `ingress.yaml`: Ingress configuration.
